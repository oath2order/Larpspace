import React from "react";
import Sidebar from "../components/Sidebar";

const Player = () => (
  <div className="player">
    <h1 className="text-center">This is the Player Page of Larpspace</h1>
    <Sidebar />
  </div>
);

export default Player;
